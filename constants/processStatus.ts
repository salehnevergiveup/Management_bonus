export enum ProcessStatus {
    PENDING = "pending",
    PROCESSING = "processing",
    COMPLETED = "completed",
    SEM_COMPLETED = "sem-completed",
    FAILED = "failed"
  }
  
export enum MatchStatus {
    PENDING = "pending",
    SUCCESS = "success",
    FAILED = "failed"
  }