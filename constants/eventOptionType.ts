export enum EventOptionType {
    MFA = "mfa",
    OTP = "otp",
    EMAIL = "email",
    SMS = "sms",
  }

export enum FormType {
    credential = "credential",
    verification = "verification",
  }
  